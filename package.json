{
    "name": "notaris-portfolio",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "cd backend && npm install && node index.js",
        "build": "cd frontend && npm install && npm run build"
    }
}